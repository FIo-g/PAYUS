# WORKLOG — 2026-06-01 · SejongPay `taehyeong` 배포 워크플로우

> Append-only 작업 로그. 절대 덮어쓰지 않으며 매 스텝 끝에 추가한다.
> 형식: `## [HH:MM] <microgoal N / step>` → ultrathink 요약 / 수행 내용 / 결과 ✅❌(evidence file:line) / 결정·ledger 상태

## 메타 정보
- 브랜치: `taehyeong` @ `a0a8a150` (= origin/taehyeong, 워킹트리 clean)
- 리모트: `https://github.com/FIo-g/PAYUS.git`
- ultragoal artifacts: `.omc/ultragoal/{brief.md,goals.json,ledger.jsonl}` (mode: aggregate)
- Goal IDs: G001-mergeverification · G002-tier1blockers · G003-tier2paymentcore · G004-verificationloop · G005-finaldeploy
- 환경 제약: native Windows → tmux 없음. `omc team`(tmux 기반) 사용 불가 → **Claude Code 네이티브 Agent**(`oh-my-claudecode:git-master` 등)로 대체 수행.
- 승인 게이트: (a) Microgoal 3 결제코어 = ultrathink→plan→**사람 승인**→구현, (b) push/merge-to-main/deploy = **사람 명시 승인** 후에만.

---

## [23:12] RULE 0 — ULTRAGOAL INIT + WORKLOG
- **ultrathink(전역 셋업 리스크):**
  - 이 prompt가 명시한 블로커(B-1~B-4, H-1~H-3)는 *이전 분석 세션의 결론*일 뿐 → 편집 전 반드시 실제 코드와 대조(fail-closed). 잘못된 전제로 수정하면 새 FE↔BE 계약 깨짐 위험.
  - 결제코어(B-1 충전, B-4 검증)는 금액·트랜잭션·idempotency·QR 변조가 얽힘 → A-4 규율 필수, 사람 승인 전 구현 금지.
  - 배포 단계는 MSW 잔존·env drift·index/seed 중복 실행·BE→FE 순서 의존이 핵심 실패점.
  - 추적 인코딩: ledger는 ASCII, 상세 한국어 근거는 본 WORKLOG(UTF-8)에 분리 기록.
- **수행:**
  - `omc ultragoal create-goals` → 5개 goal 등록 (plan/ledger 생성 확인).
  - `sejongpay/docs/WORKLOG-2026-06-01.md` 생성.
  - 가드 체크: `sejongpay/backend/`(전체 src 구조), `sejongpay/frontend/`(services/*.js, vite.config.js) 모두 실존 확인.
- **결과:** ✅ ultragoal 레저 생성 (`.omc/ultragoal/goals.json:9-54`), WORKLOG 생성, 정규 경로 가드 통과.
- **결정 / ledger 상태:** 셋업 완료. → Microgoal 1 진입 준비. (G001 = pending → 곧 in-progress)

## [23:13] Microgoal 1 — MERGE VERIFICATION · /goal 핸드오프 + ultrathink
- **/goal 핸드오프:** aggregate 조건 수신 = "Complete all ultragoal stories G001..G005". `/goal`은 네이티브 Stop-hook이라 도구로 set 불가 → ledger+WORKLOG로 durable 추적, 본 세션에서 5개 전부 구동. complete-goals가 G001을 active story로 시작함.
- **ultrathink(Microgoal 1 — 프론트 머지가 백엔드를 조용히 깨뜨릴 수 있는 지점 / 클린 머지 무결성 신호):**
  - *무엇이 깨질 수 있나:*
    1. 백엔드 파일 **삭제/리버트** — 머지 충돌 해소 실수로 backend 파일 유실·구버전 복귀.
    2. **충돌 잔여 마커**(`<<<<<<<`/`=======`/`>>>>>>>`)가 .js에 남으면 require/parse 자체가 깨짐 → Microgoal 4 테스트/빌드 실패로 직결.
    3. **package.json/lockfile 손상** — 백엔드 deps 유실 또는 lockfile 불일치.
    4. **중복/이동 파일** — `frontend/sejong-pay-design/` 중첩 트리가 `frontend/src/`와 map/pages/mocks 중복 → 머지 잔여물 의심(프론트 한정, 백엔드 무해하나 빌드 혼선 가능).
    5. `.orig` 등 머지 잔여 파일, CRLF 화이트스페이스 churn(Windows) 오탐 주의.
  - *클린 신호(증거):*
    - 트리 전체 충돌 마커 grep = 0 (특히 backend).
    - **PR#7/#8(프론트 머지) 커밋의 diffstat이 backend/ 파일을 건드리지 않음** = 백엔드 무손실·무변경 핵심 증거. 건드렸다면 red flag.
    - backend .js 50개 셋 완전(이미 확인) + 테스트 파일 존재.
    - `main...taehyeong` 분기 파악: 백엔드 변경이 *의도된* 것(db899149 인증 fix 등)인지 식별.
  - *done 정의:* 충돌 마커 0 + 백엔드 파일 무손실 + 프론트 머지가 백엔드 무변경(또는 변경이 모두 의도된 것으로 설명됨) + 머지 잔여물 없음 + main 대비 분기 명확.
  - *다른 마이크로골 영향:* 머지로 백엔드가 조용히 바뀌었다면 내가 곧 고칠 블로커(Tier-1/2)의 baseline이 틀어짐 → **머지 무결성을 가장 먼저 확정해야 downstream 전부 보호**.
- **수행(진행 중):** `git fetch --all` → 분기/가드 재확인 → git-master 에이전트(네이티브)로 독립 무결성 감사.

## [23:19] Microgoal 1 — MERGE VERIFICATION · 결과 (git-master 독립 감사 PASS)
- **ultrathink**: [23:13] 기록 완료.
- **수행**: 직접 신호 수집 후 `oh-my-claudecode:git-master`(opus, fresh-context, READ-ONLY)로 8개 체크 독립 재도출.
- **결과 ✅ PASS:**
  1. 충돌 마커 0 (추적 172파일, `git grep -E "^(<<<<<<<|=======|>>>>>>>)"`)
  2. `.orig`/`.rej` 잔여물 없음 (`git ls-files`)
  3. 백엔드 드리프트 vs main = **EMPTY** (`git diff origin/main taehyeong -- sejongpay/backend/`)
  4. PR#7(ae94d00b) 백엔드 영향 = **EMPTY** — 진짜 프론트 머지가 백엔드 무변경 (핵심 안전 신호)
  5. PR#8(a0a8a150) 백엔드 = +1897/-51 additive buildout이나 **결과 net-zero vs main**
  6. 결제/보안 코어 전부 존재·non-empty: `payment.service.js`(22209B), `wallet.service.js`(3705B), `cashback.service.js`(3173B), `qr-token.service.js`(10078B), 모델 8개, `transaction.routes.js`(1916B). ⚠ `wallet.routes.js` 없음(설계상; `user.routes.js`가 `/me/wallet*` 명시적 제외) → **B-1 충전 엔드포인트 마운트 위치를 Microgoal 3에서 확인 필수**.
  7. **taehyeong 트리가 origin/main과 byte-identical (tree SHA `89b8e6f5`).** taehyeong = main + 머지커밋 1개(net-zero). 내 수정은 main 대비 신규 디프가 되며, 블로커는 main에도 동일 존재.
  8. `sejong-pay-design/` = 추적 25파일 중복 design-scrap 서브트리(real app은 `frontend/src/` 54파일) → 프론트 빌드/MSW 혼선 가능(non-blocking) → Microgoal 4에서 처리 판단.
- **결정 / ledger 상태:** G001 verification PASS → `checkpoint --status complete`. **Microgoal 2 진입.**
- **이월 리스크:** (a) `/me/wallet*` 엔드포인트 마운트 위치 [Microgoal 3 / B-1], (b) `sejong-pay-design` 중복 [Microgoal 4 / build].

## [23:20] Microgoal 2 — TIER-1 BLOCKERS · ultrathink
- **ultrathink(5개 FE↔BE 계약 불일치 — 편집 전 양쪽 검증으로 새 계약 깨짐 방지):**
  - *불변식:* 이 블로커들은 FE service layer가 BE 실제 라우트/응답 shape와 어긋난 것. **수정은 FE만**(BE 변경 금지 — 다른 소비자/테스트 깨짐 위험). 단, BE가 정말 target shape인지 *읽어서 확인* 후 정렬.
  - **B-2 토큰:** 처방 `res.data.data.{accessToken,refreshToken}` + `return {data:user}`. → BE 응답 envelope가 `{success,data:{...}}`(CLAUDE.md rule5)라는 전제. **확인 필요:** auth.controller login/register/refresh가 `data` 안에 정확히 무엇을 중첩하는가(accessToken/refreshToken/user 키명), 그리고 auth store(소비자)가 `{data:user}`를 읽는가(FE 내부 계약도 깨지면 안 됨).
  - **B-3 proxy:** 처방 5000→4000. **확인 필요:** BE 실제 PORT(env.js/server.js 기본값)가 4000인가.
  - **H-1 쿠폰:** `/coupons`→`/coupons/me`, `/coupons/template`→`/coupons/templates`. **확인 필요:** coupon.routes 실제 경로. 함수 의도(내 쿠폰 vs 템플릿) 오매핑 주의 — 맹목 치환 금지.
  - **H-2 스탬프:** `/stamps`→`/stamps/me`, `/stamps/:id`→`/stamps/me/:id`. **확인 필요:** stamp.routes 실제 경로.
  - **H-3 알림:** PATCH→PUT(markAsRead, markAllAsRead). **확인 필요:** notification.routes 실제 메서드/경로.
  - *교차 의존:* api.js baseURL prefix(`/api/v1` 등)와 라우트 마운트(app.js) — FE 상대경로 + baseURL = BE 풀경로 정합 확인. MSW 핸들러가 구 shape면 MSW-on dev 깨짐(secondary, 플래그).
  - *done 정의:* 5개 FE 파일이 BE 실제 라우트/메서드/응답파싱과 일치, 신규 미스매치 0, proxy가 실제 BE 포트.
  - *접근:* BE+FE 진실을 **중앙에서 1회 검증** 후 surgical edit를 병렬 배치 적용. (5개 redundant 서브에이전트 fan-out 대신 — 각자 BE 재읽기 시 shape 가정 불일치 위험. "lightest path that preserves quality" 원칙. 독립 재검증은 Microgoal 4 ralph build/test 레인이 담당.)
- **수행(진행 중):** 13개 ground-truth 파일 병렬 read → 처방 검증 → 편집.

## [23:27] Microgoal 2 — TIER-1 BLOCKERS · 결과 (5개 적용·검증 PASS)
- **수행**: BE+FE ground-truth 13파일 + 소비자(`AuthContext.jsx`, `LoginPage.jsx`, `response.js`) 중앙 검증 후 6개 편집(파일당 1회) 병렬 적용.
- **처방 검증 결과 — 5개 모두 BE 실제와 일치, 새 계약 깨짐 0:**
  - **B-2 토큰** (`auth.service.js:5-15`, `api.js:56`): `ok()`=`{success,data}`(response.js:16) 확정. login/register → `setTokens(res.data.data.accessToken, res.data.data.refreshToken)` + `return {data: res.data.data.user}`. api.js refresh → `setTokens(data.data.accessToken, ...)`. ✅ 소비자 정합: `AuthContext.jsx:34,40` `setUser(res.data)`=user, `LoginPage.jsx:32-33` `res.data.name`/`res.data.role` (기존 잠재버그까지 복구).
  - **B-3 proxy** (`vite.config.js:16`): `5000`→`4000` (= `env.js:13` PORT 기본 4000). ✅
  - **H-1 쿠폰** (`coupon.service.js:5,8`): `/coupons`→`/coupons/me`(`coupon.routes:11`), `/coupons/template`→`/coupons/templates`(`coupon.routes:13`). ✅
  - **H-2 스탬프** (`stamp.service.js:5-6`): `/stamps`→`/stamps/me`(`stamp.routes:10`), `/stamps/${merchantId}`→`/stamps/me/${merchantId}`(`stamp.routes:12`). ✅
  - **H-3 알림** (`notification.service.js:6-7`): `.patch`→`.put` markAsRead/markAllAsRead (`notification.routes:12-13` PUT). ✅
- **결과 ✅ PASS**: 정적 grep — stale 패턴(`localhost:5000`, `res.data.accessToken`, `.patch(`, `/coupons'`, `/stamps'`, `/coupons/template'`) **0건**; 신규 패턴 전부 존재. (빌드/타입 검증은 Microgoal 4 ralph.)
- **관찰(비수정·이월)**: `LoginPage.jsx:33` `navigate('\merchant\home')` 백슬래시 경로버그 — Tier-1 범위 밖·권세현 영역 → 미수정, 팀 보고용 플래그.
- **결정 / ledger 상태:** G002 verification PASS → checkpoint complete. **Microgoal 3(결제코어, A-4 승인 게이트) 진입.**

## [23:30] Microgoal 3 — TIER-2 PAYMENT CORE · ultrathink (a-priori, A-4 게이트)
> 구현 전. RULE 2 + A-4: ultrathink→plan→**사람(김태형) 승인**→구현. 아래는 코드 정독 전 위험 프레이밍.
- **B-1 충전 `POST /users/me/wallet/charge` (잔액 증가 = 돈 생성, 고위험):**
  1. *정수 금액:* `Number.isInteger(amount) && amount>0` 강제. float/음수/0/NaN 거부. (상한선 검토)
  2. *세션 트랜잭션:* 잔액 증가 + Transaction(type:'charge') 삽입을 **원자적**으로(rule2/6). 하나만 성공하면 잔액캐시≠거래합산 드리프트 버그.
     - ⚠ MongoDB 트랜잭션은 replica set 필요 → mongodb-memory-server 테스트가 replSet 지원하는지 확인(기존 payment 테스트가 통과하므로 지원 추정, 검증).
  3. *멱등성(app-level):* idempotency unique index는 payment 전용(부분 인덱스 추정) → charge는 DB 제약 의존 불가. key 제공 시 기존 charge 거래 조회 후 반환. ⚠ 동시 중복 제출 race: unique index 없이 check-then-insert는 완전 안전 X. 옵션: (a) charge 포함 partial unique index 추가(=모델 변경, 강동한), (b) atomic claim. **task가 "app-level"로 스코프** → app-level 구현 + 동시성 한계 명시 + 인덱스 옵션 제시.
  4. *creditBalance 재사용:* 시그니처(session 인자? Math.floor? 잔액 atomic?) 및 내부에서 Transaction을 만드는지(중복 생성 방지) 확인 필수.
  5. *append-only:* Transaction(type:'charge') 필수필드(transactionNo/balanceBefore/After/status) + **type enum에 'charge' 존재 여부** 확인. 없으면 모델 변경(강동한) 필요 → 플래그.
  6. *인증:* req.user._id (최근 정합 커밋). 미들웨어 확인.
  7. *FE 정합:* FE가 charge를 정확히 어느 경로/shape로 호출하는지(user.service vs transaction.service, Charge 페이지) 확인 → BE를 FE에 맞춤.
- **B-4 결제검증 `POST /transactions/payment/verify` (read-only preview):**
  1. ⚠⚠ *nonce 소비 함정:* `verifyQrToken`이 nonce를 1회용 소각하면, verify 후 실제 결제의 재검증이 실패. **read-only는 nonce를 태우면 안 됨.** qr-token.service에 비소비(peek) 경로/플래그가 있는지 확인 → 없으면 플랜에서 해결(HMAC+만료+nonce존재만 검사, 소각 X).
  2. *반환:* verifyQrToken→merchantId→Merchant 조회→`{merchantName, category, cashbackRate}`. merchant 없으면 MERCHANT_NOT_FOUND. Merchant 필드명 확인.
  3. *무변경:* 거래 생성·nonce 소각 등 DB mutation 절대 없음.
  4. *인증/마운트:* transaction.routes(김태형 영역, 인증됨) 하위.
- **교차 영향:** B-4 nonce 설계가 Microgoal 4 e2e(login→charge→QR결제→내역)의 결제 단계 성패를 좌우. type enum 'charge' 누락 시 모델 변경 의존.
- **수행(진행 중):** 핵심 결제코어 BE/FE 파일 병렬 정독 → 코드 근거로 플랜 확정 → 제시 후 **승인 대기(STOP)**.

## [23:36] Microgoal 3 — 결제 코어 · 코드근거 PLAN (승인 대기 STOP)
- **코드근거 핵심 발견(31파일 정독):**
  - B-4 nonce 함정 **이미 해결**: `verifyDynamicQrToken(token,secret)`는 순수 crypto(서명+만료+nonce존재)만·DB무관(qr-token.service:144-189); 소각은 별도 `consumeNonce()`. 실제 함수명=`verifyDynamicQrToken`(처방의 verifyQrToken은 부정확).
  - Transaction.type enum에 `'charge'` 존재(model:23) → **모델 변경 불필요**.
  - idempotency unique = `type:'payment'` 부분인덱스(model:110-119) → charge는 app-level. **FE는 charge에 key 미전송**(user.service:8).
  - `creditBalance`(wallet.service:60)=정수검증+원자$inc, Transaction 생성 안 함 → charge가 별도 append.
  - 소비자 계약 확정: ChargePage는 charge 응답 무시·`refreshUser()`로 잔액 재조회(응답 shape 자유). PaymentConfirmPage verify는 `merchant.merchantName/category/cashbackRate` 사용(line102-103,66) → B-4 반환 `{merchantName,category,cashbackRate}`. 실제 결제는 `idempotencyKey: crypto.randomUUID()` 전송(line83, e2e 정상).
- **PLAN (전 파일 김태형 결제코어 영역):**
  - **B-1 충전**: CREATE `routes/wallet.routes.js`(POST /me/wallet/charge), `controllers/wallet.controller.js`(thin), `utils/tx-no-generator.js`(payment.service 미변경); MODIFY `services/wallet.service.js`(+`chargeWallet`: `session.withTransaction`→creditBalance+Transaction.create(type:'charge',status:'completed',balanceBefore/After,txno retry); optional app-level idempotency); MODIFY `app.js`(walletRouter를 userRouter 뒤 `/api/v1/users` 마운트).
  - **B-4 검증**: MODIFY `services/payment.service.js`(+read-only `verifyPaymentQr({qrToken})`: parseClaimedPayload→Merchant.findById→verifyDynamicQrToken→merchantId 일치 확인→`{merchantName,category,cashbackRate}`; **consumeNonce 호출 안 함**); MODIFY `controllers/transaction.controller.js`(+`verifyPayment`); MODIFY `routes/transaction.routes.js`(+POST /payment/verify).
  - **A-4 self-check**: 정수(Number.isInteger·amount>0·≤cap), session withTransaction, append-only, idempotencyKey(charge app-level), timingSafeEqual(verifyDynamicQrToken 재사용).
  - **미세 트레이드오프**: walletRouter 마운트로 wallet 경로에 인증 1회 추가 실행(무해); tx-no 알고리즘 util/payment.service 중복(payment core 미변경 위함).
- **상태**: 구현 보류. **사람 승인 게이트(AskUserQuestion)** — 승인 시 구현 진행. (ledger G003 in-progress 시작 예정)

## [23:55] Microgoal 3 — 결제 코어 · 구현 + fresh-context 리뷰 (A-4 PASS)
- **승인**: 사람 승인 게이트 통과 — 계획대로 구현 / charge 멱등성=optional app-level / 지갑조회(GET wallet·transactions) 추가.
- **구현(8파일)**: NEW `utils/tx-no-generator.js`·`controllers/wallet.controller.js`·`routes/wallet.routes.js`; MOD `services/wallet.service.js`(+chargeWallet), `services/payment.service.js`(+verifyPaymentQr **read-only, processPayment 불변**), `controllers/transaction.controller.js`(+verifyPayment), `routes/transaction.routes.js`(+POST /payment/verify), `app.js`(walletRouter mount).
- **구문검증**: `node --check` 9/9 OK(8파일 + M2 재검증).
- **fresh-context 독립리뷰 2개(opus, 병렬) — 둘 다 PASS, CRITICAL/HIGH 0:**
  - code-reviewer: chargeWallet 정수/cap/원자성(withTransaction)/balance math/append-only/txno retry/handle-reset PASS; verifyPaymentQr read-only·**nonce 미소비**·processPayment 동일검증·에러매핑 PASS; 라우팅 무shadowing; FE↔BE 정확일치; A-4 self-check 전부 PASS.
  - security-reviewer: money inflation 불가(정수/cap/no-coercion), cross-account 불가(userId=req.user._id), IDOR 없음(owner-scoped), QR보안 유지(HMAC+expiry+timingSafeEqual, nonce 미소비/미probe), injection-safe, prod 에러 누출 없음. PASS.
- **즉시 수정 M2**: `transaction.routes.js` verify에 `paymentLimiter` 추가(enumeration/CPU 남용 차단). node --check OK.
- **이월·플래그(비차단, 사전프로덕션 검토):**
  - M1 requireVerified/role gate — 정책결정 + `/payment`에도 pre-existing 부재 + 시드/테스트 verified 여부 미확인(무리 추가 시 e2e 위험) → 팀 결정 보류.
  - M3 월충전한도 + `walletMonthlyCharged` 미증가 — 정책값+월리셋잡=강동한 도메인 → 이월.
  - M4(=code MEDIUM-1) charge 멱등 동시 race — **승인된 트레이드오프**; FE는 charge에 key 미전송→실질 무영향; 완전안전엔 partial unique index 확장(강동한).
  - MEDIUM-2 `TransactionPage.jsx:27` `res.data.transactions` vs BE `{items}` = **pre-existing**(내 변경 무관). getMyTransactions 와이어링 시 `res.data.items` 사용 권장.
  - L1 limit NaN(기능안전)/L2 body schema(defense-in-depth)/L3 CORS null-origin(pre-existing)/L4 npm audit transitive HIGH(빌드타임·미도달) → M4·배포에서 점검.
- **결정 / ledger**: A-4 self-check + fresh-context 리뷰 PASS → **G003 complete**. 동시성/빌드/e2e 실검증은 Microgoal 4(ralph). (push/merge는 별도 승인)

## [23:58] Microgoal 4 — VERIFICATION LOOP · ultrathink (직접 diagnose→fix→verify, 통제형 ralph)
- **백엔드 test(`npm ci && jest --runInBand`):**
  - lockfile 존재 → npm ci OK. mongodb-memory-server ^10 — withTransaction(chargeWallet/processPayment)은 **replSet 필요**; 기존 payment 테스트가 통과하므로 harness가 replSet 지원 추정(확인).
  - 회귀 위험: app.js가 walletRouter→wallet.controller→transaction.service+wallet.service→tx-no-generator 로드. 전부 require 성공해야(구문·export 확인됨). deduct/creditBalance는 verbatim 보존(동작 동일).
  - transaction.routes.test.js(supertest)가 새 /payment/verify로 기존 assertion 깨지지 않는지. mongodb-memory-server 바이너리 최초 다운로드 느림.
- **프론트 build(`npm ci && vite build`):**
  - vite build=esbuild(타입체크 없음) → 타입 관대. **가장 유력 실패=recovered 프론트의 미해결 import**(components/layout/PageLayout, components/common/* 등 누락 가능).
  - `sejong-pay-design/` 중복 서브트리는 entry-driven 빌드그래프에 없으면 무영향(확인). Tier-1 서비스 편집은 plain JS → 빌드 무영향.
- **e2e smoke(MSW off):** Atlas + BE/FE 구동 + 브라우저 필요 → 헤드리스·무자격증명 환경에서 불가 가능성. backend route 통합테스트(supertest+in-memory)가 API레벨 e2e 대체. 브라우저 e2e는 M5 배포후 smoke로 이월(Atlas 연결 시 시도).
- **전략:** 백엔드 npm ci+test(자기완결, in-memory) 먼저 → 프론트 npm ci+build. 내 변경發 회귀는 즉시 수정; 프론트 pre-existing 빌드 블로커(권세현 도메인)는 배포에 필요한 최소만 수정 or 플래그. most-likely-failure-first.
- **수행(진행 중):** 백엔드·프론트 npm ci 백그라운드 병렬 시작 → 완료 후 test/build.

## [00:08] Microgoal 4 — VERIFICATION LOOP · 결과 (ALL GREEN)
- **백엔드 진단→수정**: `npm ci` 실패 → **package-lock.json 불일치**(express-validator@7.3.2 등 lock 누락, **pre-existing 배포 블로커**, 내 변경 무관) → `npm install`로 lock 동기화(+29줄, package.json 불변).
- **백엔드 test** `jest --runInBand`: **5 스위트 / 39 테스트 PASS**
  - payment.service 12(동시성·무결성: 5×idempotency·과다차감 race·잔액부족 완전롤백·nonce replay·캐시백 floor·append-only 원장재구성·만료QR/쿠폰),
  - transaction.routes 3(인증배선·owner-scope), transaction.query 15,
  - **신규 wallet-verify 8**, **신규 e2e-smoke 1**. 회귀 0(기존 30 그대로 그린).
- **신규 테스트(A-4 테스트 규율 충족)**:
  - `wallet-verify.test.js`: chargeWallet 정수/cap/원자성/append-only/순차멱등 + verifyPaymentQr 가맹정보반환/변조거부/미존재가맹점거부/**nonce 미소비 후 결제성공** 불변식.
  - `e2e-smoke.test.js`: 전체 app.js supertest — register→charge→verify→pay→wallet(7060원)→history(charge+payment+cashback)→receipt. 로그: `/me/wallet/charge 201`·`/payment/verify 200`·`/payment 201`(**verify 직후 결제 성공 = nonce 미소비 실증**).
- **프론트 build**: `npm ci`(340pkg) + `vite build` → **dist 생성, 그린**(3141 modules; 경고는 chunk크기/dynamic import 뿐, 무실패).
- **라이브 e2e(Atlas/브라우저)**: 무자격증명·헤드리스 → 미실행. **API레벨 e2e가 동일 여정 대체 검증**. 브라우저+Atlas smoke는 M5 배포후로 이월.
- **결정 / ledger**: 백엔드 test ✅ + 프론트 build ✅ + API e2e ✅ → **G004 complete**. (push/deploy는 별도 승인)

## [00:12] Microgoal 5 — FINAL VERIFICATION + DEPLOY · ultrathink (배포 리스크)
- **배포 실패점 reasoning:**
  1. **MSW 프로덕션 잔존** — 프론트가 prod에서 MSW worker를 켜면 모든 API가 mock에 가로채여 앱 무기능. main.jsx의 MSW 게이팅(VITE_USE_MSW/dev 한정) 확인 필수.
  2. **env drift** — BE: MONGODB_URI(Atlas)·JWT_*·JWT_*_EXPIRES·ALLOWED_ORIGINS(배포 프론트 origin 포함해야 CORS 통과)·QR_EXPIRY_MINUTES. FE: VITE_API_BASE_URL=배포 BE URL+/api/v1. **BE ALLOWED_ORIGINS ↔ FE VITE_API_BASE_URL 정합 필수**.
  3. **index/seed "정확히 1회"** — migrate-indexes가 Atlas에 **부분 unique idempotency 인덱스 + nonce unique 인덱스**를 빌드해야 결제 동시성 안전이 성립(테스트가 명시 빌드). prod에서 mongoose autoIndex off면 자동 안 됨 → migrate-indexes 필수. seed 중복실행 시 데이터 중복 위험 → 1회만(또는 --reset).
  4. **BE→FE 순서** — BE 먼저 배포→URL 확보→FE VITE_API_BASE_URL 주입→FE 빌드/배포. Atlas migrate+seed는 BE 서빙 전.
  5. **rollback** — 승인 전 taehyeong 미push 유지; 플랫폼 이전 배포 보존; 알려진 good 커밋에서 배포.
  6. **시크릿** — JWT/Atlas는 플랫폼 env로(커밋 금지). .env gitignore 유지. vercel/railway.json에 시크릿 없음.
  7. **start/PORT** — BE start=node src/server.js, server가 process.env.PORT(Railway 주입) 사용해야. Node 20.
- **제약**: 실제 배포 = 명시 승인 + Atlas/Vercel/Railway 자격증명 필요 → 자율 배포 불가. **검증+설정준비+승인게이트**로 진행, 라이브 smoke 전 G005 미완료(fail-closed).
- **수행(진행 중)**: 배포 파일 정찰 → git-master 최종 drift 검증 → 배포설정 생성·커밋 → 승인/자격증명 요청(STOP).

## [00:18] Microgoal 5 — 최종검증 + 배포설정 준비 (배포 = 승인 게이트 STOP)
- **git-master 최종 drift 검증: PASS** — 배포후보 = 검증된 빌드. 클린트리(WORKLOG만 untracked), 4커밋 정합, 17파일 in-scope(models 무유출), 충돌마커0, ahead4/behind0 무rewrite, lockfile-only(package.json 불변).
- **prod MSW off 보장**: `main.jsx`가 MSW worker 미시작(grep 0매치).
- **env 확정**: BE 필수 `NODE_ENV=production·MONGODB_URI·JWT_ACCESS_SECRET·JWT_REFRESH_SECRET·ALLOWED_ORIGINS`(+PORT 자동, 선택 JWT만료·QR_EXPIRY_MINUTES). FE 필수 `VITE_API_BASE_URL` 단일. HMAC_QR_SECRET/KAKAO/FCM 미사용.
- **배포설정 생성+커밋(`0e98dcd5`)**: `backend/railway.json`+`Procfile`, `frontend/vercel.json`(SPA rewrite)+`.env.example`, `docs/deployment-guide.md`(runbook). JSON 유효성 검증 통과. 검증된 앱코드 불변(플랫폼 설정/문서만).
- **배포 순서(runbook)**: BE(Railway) → **Atlas migrate-indexes 1회**(prod autoIndex off → 부분 unique idempotency·nonce unique 빌드 필수) → seed 1회(멱등) → FE(Vercel, VITE_API_BASE_URL) → ALLOWED_ORIGINS 갱신·BE 재배포 → smoke.
- **🚧 BLOCKED (fail-closed)**: 실제 배포 = **사람 명시 승인 + Atlas/Railway/Vercel 자격증명** 필요 → 자율 배포 불가. push도 승인 게이트. **G005 미완료 유지**(라이브 smoke 통과 전까지 complete 금지). → 승인·배포방식 질의.

### 📊 현재 종합 상태 (배포 승인 대기)
| MG | 내용 | 상태 |
|----|------|------|
| 1 | 머지 무결성 | ✅ complete |
| 2 | Tier-1 블로커 5건 | ✅ complete |
| 3 | 결제코어 B-1/B-4 (A-4) | ✅ complete |
| 4 | 검증 루프(39테스트+빌드+e2e) | ✅ complete |
| 5 | 최종검증+배포 | 🚧 설정·검증 완료, **배포 승인 대기** |

로컬 커밋(taehyeong, 미push): `78340876`(Tier-1) `d602081c`(Tier-2) `ec81e3e1`(lockfile) `7b4627ef`(tests) `0e98dcd5`(deploy). 다음: 사람 승인 후 push/배포.

## [00:22] 최종 종합 요약 (FINAL SUMMARY — 배포 보류 결정)
> 사람 결정: **push 승인 / 배포 보류(준비만 완료)**. migrate-indexes·seed·배포 미실행. **G005 미완료 유지(fail-closed)**.

### 완료된 작업 (Microgoal 1~4 ✅ + 5 준비)
1. **머지 무결성(G001 ✅)**: git-master 독립감사 — 충돌마커0, PR#7 백엔드 무변경, taehyeong 트리=origin/main byte-identical, 결제/보안 코어 전부 존재.
2. **Tier-1 블로커 5건(G002 ✅)** — 커밋 `78340876`: B-2 토큰(`res.data.data`+`{data:user}`, AuthContext/LoginPage 정합), B-3 proxy(5000→4000), H-1 쿠폰(`/me`·`/templates`), H-2 스탬프(`/me`·`/me/:merchantId`), H-3 알림(PATCH→PUT). 전부 BE 라우트 대조 검증.
3. **결제코어 B-1/B-4(G003 ✅, A-4)** — 커밋 `d602081c`: chargeWallet(정수·cap·withTransaction·append-only·txno retry·optional app-level 멱등), verifyPaymentQr(read-only·**nonce 미소비**·processPayment 동일검증). 사람 승인 게이트 통과 + fresh-context 리뷰 2건(정확성·보안) PASS.
4. **검증 루프(G004 ✅)** — 커밋 `ec81e3e1`(lockfile fix), `7b4627ef`(신규 테스트): 백엔드 **5스위트 39테스트 PASS**(payment 동시성12 + wallet-verify 8 + e2e 1 등), 프론트 `vite build` 그린, API e2e(register→charge→verify→pay→history→receipt) 그린.
5. **배포 준비(G005 🚧)** — 커밋 `0e98dcd5`: railway.json/Procfile/vercel.json/.env.example/deployment-guide.md. git-master 최종 drift PASS. prod MSW off 확인. **배포 보류**.

### 남은 작업 (배포 — 사람 진행)
`docs/deployment-guide.md` 순서: BE(Railway) → **Atlas migrate-indexes 1회**(prod autoIndex off → 결제 멱등/nonce 인덱스 필수) → seed 1회(멱등) → FE(Vercel, VITE_API_BASE_URL) → ALLOWED_ORIGINS 갱신·BE 재배포 → 배포 URL smoke. 완료 후 G005 checkpoint.

### 이월 보안/품질(비차단, 사전프로덕션 검토 — 강동한/팀 협의)
- M1 charge/payment `requireVerified`·role 게이트(정책; /payment 도 pre-existing 부재).
- M3 월 충전 한도 + `walletMonthlyCharged` 적립(모델/리셋잡).
- M4 charge 멱등 동시 race → partial unique index 확장(모델).
- pre-existing: `TransactionPage.jsx:27` `res.data.transactions`(BE는 `{items}`), `LoginPage.jsx:33` `'\merchant\home'` 백슬래시 경로, `sejong-pay-design/` 중복 서브트리(25파일), npm audit transitive HIGH(빌드타임).

### Push
사람 승인 → `origin/taehyeong` 에 push(6커밋: Tier-1·Tier-2·lockfile·tests·deploy·docs). main 머지는 별도(사람).

<!-- APPEND-BELOW -->
